{
  "name": "bitespeed-server",
  "version": "1.0.0",
  "description": "Professional Express server with PostgreSQL connection and contact management",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "echo \"Add ESLint configuration for linting\"",
    "clean": "rm -rf node_modules package-lock.json && npm install"
  },
  "keywords": ["express", "postgresql", "nodejs", "rest-api", "contact-management"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "pg": "^8.11.3",
    "dotenv": "^16.3.1",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
